<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="@color/taskbarBackground">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Screen Cast"
            android:textColor="@color/onSurface"
            android:textSize="18sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnRefresh"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Refresh"
            android:textSize="12sp"
            android:background="@drawable/bg_btn_secondary"
            android:textColor="@color/onSurface"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/btnDisconnect"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Disconnect"
            android:textSize="12sp"
            android:background="@drawable/bg_btn_primary"
            android:textColor="@color/onPrimary"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Status -->
    <TextView
        android:id="@+id/tvCastStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Not casting"
        android:textColor="@color/onSurface"
        android:textSize="13sp"
        android:padding="16dp" />

    <!-- Device list -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:id="@+id/castDeviceContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp" />
    </ScrollView>

    <!-- Help text -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Supports Miracast, Chromecast, and DLNA receivers.\nMake sure your TV/display is on the same WiFi network."
        android:textColor="#666"
        android:textSize="11sp"
        android:padding="16dp"
        android:gravity="center" />
</LinearLayout>